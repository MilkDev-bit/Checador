[build]
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "./paselista"
  healthcheckPath = "/api/health"
  healthcheckTimeout = 120
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 3
